import React from "react";
import { Typography, Container } from "@mui/material";

const Projects = () => {
  return (
    <Container>
      <Typography variant="h2">Projects</Typography>
      <Typography variant="body1">This is the Projects page.</Typography>
    </Container>
  );
};

export default Projects;
