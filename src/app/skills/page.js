import React from "react";
import { Typography, Container } from "@mui/material";

const Skills = () => {
  return (
    <Container>
      <Typography variant="h2">Skills</Typography>
      <Typography variant="body1">This is the Skills page.</Typography>
    </Container>
  );
};

export default Skills;
